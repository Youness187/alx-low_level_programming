#include "main.h"
/**
 * print_alphabet - function that prints alphabet in lowercase
 *
 * Return: always 0;
 */
void print_alphabet(void)
{
for (char i = 'a'; i <= 'z'; i++)
{
_putchar(i);
}
_putchar('\n');
}